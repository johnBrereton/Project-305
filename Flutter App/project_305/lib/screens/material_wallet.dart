import 'package:flutter/material.dart';

class MaterialWallet extends StatelessWidget {
  const MaterialWallet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return const Scaffold(
      body: Center(
        child: Text("This is the Wallet Screen on Android"),
      ),
    );
  }
}